---
title: "AI時代のRust crate選定 (4/4) GUI crate"
emoji: "🦀"
type: "tech"
topics: ["rust", "gui", "egui", "tauri"]
published: false
publication_name: estie
---

連載最終回は **GUI crate** です。これまで（[第1回](#)デファクトcrate群、[第2回](#)Web/DB、[第3回](#)時刻）と違い、GUIは「正解の選択肢」が用途で大きく変わる、もっとも割れる領域です。

それでも、AIに書かせる前提で選定基準を立てると、見えてくる景色はシンプルです。結論を先に言うと、GUIのcrate選定は次の2点でほぼ決まります。

1. **ビルドが通る環境を作れるか**（Pure Rust か、外部のCライブラリや WebView が要るか）
2. **AIの修正ループが回るか**（GUIは進化が速く一発では当たらない。外したときに直せるか）

実際に最新版へ書かせて検証すると、意外な結果が出ました。**即時モードで「AIと相性が良い」とされる egui ですら、一発書きは 0/5（全滅）**だったのです。理由は後述しますが、これは「GUIは一発書きの精度で選んではいけない」ことを示しています。

# 8つの観点（GUI版）

第2回から使っている8観点を再掲します。見るのは「**AIに書かせたときに回りやすいか**」です。

| 観点 | チェック内容 | NG例 |
|---|---|---|
| AI相性 | AIが現在のAPIで動くコードを書ける | 旧バージョンのAPIを出す |
| 初速 | READMEから10分で動く | CLI導入や設定ファイルで詰まる |
| 復旧性 | エラーをAIに渡すと直る | 原因が環境で、コードでは直らない |
| 定番接続 | tokio/serde等と繋がる | 独自世界観が強すぎる |
| ビルド | 自分の環境で素直にbuildできる | C依存・WebView前提で詰まる |
| 速度 | cargo check が待てる | 依存が重くビルドが長い |
| 捨てやすさ | adapterで隔離できる | UI構造がアプリ全体に染み込む |
| 保守 | 外部変化に追従している | 開発停止 |

GUIで特に効くのは **ビルド**（C依存・外部依存があるか）、**AI相性**（学習データがそのバージョンに追いついているか）、**復旧性**（外したときコンパイラが導いてくれるか）の3つです。

# GUIは設計思想で4つに分ける

GUIライブラリは数が多いので、設計思想の代表でまず4つに整理します。加えて、C依存の教材として gtk4 を見ます。

| パラダイム | crate | 描き方 |
|---|---|---|
| 即時モード | [egui](https://crates.io/crates/egui)（[eframe](https://crates.io/crates/eframe)） | 毎フレームUIを記述。状態管理が単純 |
| Elm Architecture | [iced](https://crates.io/crates/iced) | state / Message / update / view に分ける |
| 宣言的DSL | [slint](https://crates.io/crates/slint) | 独自の `.slint` 言語でUIを書きコンパイル |
| Web技術 | [tauri](https://crates.io/crates/tauri) | HTML/CSS/JS + Rust。OSのWebViewを使う |
| ネイティブ(C) | [gtk4](https://crates.io/crates/gtk4) | GTK4へのバインディング |

※バージョンは eframe/egui 0.34 / iced 0.14 / slint 1.16 / tauri 2.11 / gtk4 0.11（2026年6月時点）

ここでも見るのは「描画の美しさ」ではなく「AIに書かせる前提の回りやすさ」です。同じカウンタ（+1 / -1 ボタンと現在値）を各crateで一発書きさせ、`cargo build` の通過で評価しました。

## egui（eframe）— 即時モード

egui は「毎フレーム、いまのUIを全部記述する」即時モードです。状態のツリーを管理しなくてよく、AIとの相性が良いと一般に言われます。

```rust
// eframe 0.34（2026年時点）の正しい書き方
use eframe::egui;

struct CounterApp { count: i64 }

impl eframe::App for CounterApp {
    fn ui(&mut self, ui: &mut egui::Ui, _frame: &mut eframe::Frame) {
        ui.label(format!("Count: {}", self.count));
        if ui.button("+1").clicked() { self.count += 1; }
        if ui.button("-1").clicked() { self.count -= 1; }
    }
}

fn main() -> eframe::Result<()> {
    eframe::run_native("Counter", eframe::NativeOptions::default(),
        Box::new(|_cc| Ok(Box::new(CounterApp { count: 0 }))))
}
```

ところが、このコードには罠があります。**多くの学習データにある `fn update(&mut self, ctx, frame)` 形式は、eframe 0.34 ではもうコンパイルが通りません**（必須メソッドが `ui` に刷新されました）。これが後述の「0/5」の原因です。

## iced — Elm Architecture

iced は state / Message / update / view に分ける、Elm由来のアーキテクチャです。状態遷移が型（`Message` enum）で表現され、規律ある設計ができます。

```rust
// iced 0.14 の正しい書き方（application の第1引数が状態初期化関数）
fn main() -> iced::Result {
    iced::application(Counter::default, Counter::update, Counter::view)
        .title("Counter")
        .run()
}
```

iced も罠があります。**0.14 で `application` の第1引数が「タイトル文字列」から「状態初期化関数」に変わりました**。`iced::application("Counter", ...)` という旧来の形は通りません。

## slint — 宣言的DSL

slint は独自の `.slint` 言語でUIを宣言的に書き、ビルド時にネイティブコードへコンパイルします。`slint!` マクロを使えば Rust ファイル内に直接書けます。

```rust
slint::slint! {
    import { Button, VerticalBox } from "std-widgets.slint";
    export component MainWindow inherits Window {
        // ... プロパティとコールバック
    }
}
```

独自DSLなので学習データは相対的に薄いはずですが、後述のとおり一発書きは健闘しました。なお slint は **ライセンスが3択**（Royalty-Free / GPLv3 / 商用）で、配布形態によっては自分のコードのライセンスに影響する点は選定上の注意です。

## tauri — Web技術 + OS WebView

tauri はフロントエンドをHTML/CSS/JSで書き、RustバックエンドとOSのWebView（macOSなら WKWebView）を組み合わせます。フロントの学習データは膨大で、バイナリにブラウザエンジンを同梱しないため小さくなります（Electronとの大きな違い）。

```rust
#[tauri::command]
fn increment(value: i64) -> i64 { value + 1 }

fn main() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![increment])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

ただし tauri は**最小構成でも段取りが多い**（後述）。

# 8つの観点で評価する

最新版で実際に書かせて採点した結果（下の実測メモ）を反映した評価です。

| 観点 | egui | iced | slint | tauri | （gtk4） |
|---|---|---|---|---|---|
| AI相性 | △ 一発書き 0/5（新API未追従） | △ 0/5（新API未追従） | ○ 4/5 | ○ Web技術は学習データ厚い | ○ 機械的だがビルド前で止まる |
| 初速 | ○ eframe雛形で即 | ○ 雛形あり | ○ `slint!`で完結 | △ CLI/設定/アイコン/HTMLが要る | △ GTK導入が前提 |
| 復旧性 | ◎ ヒントで1ラウンド | ◎ ヒントで1ラウンド | ◎ DSLエラーで1ラウンド | ○ Rust側は直る | △ 原因がC依存で環境ループ |
| 定番接続 | ○ | ○ tokio等 | △ DSL世界観 | ○ Web資産/tokio | △ GTK世界観 |
| ビルド | ◎ Pure Rust・素で通る | ◎ Pure Rust・素で通る | ○ Pure Rust・素で通る | △ Xcode/WebView/設定一式 | ✗ pkg-config+GTK必須・素で不可 |
| 速度 | ○ 208依存 | ○ 191依存 | △ 335依存 | △ 272依存 | — |
| 捨てやすさ | ○ | △ MVUが染みる | △ DSL層 | ○ フロント/バック分離 | △ |
| 保守 | ◎ 活発 | ◎ 活発 | ◎ 活発 | ◎ 活発 | ◎ 活発 |

※ 一発書きの精度を実測したのは、単一バイナリで完結する egui / iced / slint です。tauri（Rust＋Web＋設定の複合）と gtk4（環境構築で止まる）は一発書きの粒度に合わないため、ビルド・依存・環境の観点で評価しています。

## AI相性・復旧性 — 一発書きは全滅、でもヒントで即復旧

学習データの所在から、当初は「egui（即時モードで単純）と tauri（Web技術で学習データ豊富）が一発書きに強い」と予想していました。実測は予想を裏切りました。

- **egui: 0/5**。5試行すべてが、eframe 0.34 で廃された旧 `fn update` 形式で書き、`E0046 missing ui` でコンパイルエラー
- **iced: 0/5**。5試行すべてが、0.14 で変わった `application` の第1引数を旧来のタイトル文字列で渡し、`E0277 &str: BootFn` でコンパイルエラー
- **slint: 4/5**。独自DSLながら健闘。失敗1件も `std-widgets` の import 漏れ

つまり **GUIは進化が速く、最新版でAPIが変わると、学習データが追いつかず一発では当たらない**。「即時モードだからAI相性が良い」という一般論は、バージョンが動くと崩れます。

ただし重要なのは、**失敗が全てコンパイルエラーだった**ことです（第3回の jiff のような silent failure はゼロ）。そして、コンパイラのエラー出力だけを返して再修正させたところ——

:::message
**実測メモ**（Claude Opus 4.8 / egui・iced・slint 各5試行 / ドキュメント参照を禁じた「一発書き」/ `cargo build` の通過で判定）

- 一発書き合格: **egui 0/5・iced 0/5・slint 4/5**
- 失敗の主因: egui = eframe 0.34 で `App` の必須メソッドが `update` → `ui` に刷新／iced = 0.14 で `application` の第1引数がタイトル文字列 → 状態初期化関数に破壊的変更／slint = `std-widgets` の import 漏れ1件。**全てコンパイルエラー（silent failure はゼロ）**
- 修正ループ: 失敗11件すべて、**コンパイラの出力（`help:` 行を含む）だけを返したら1ラウンドで全て build 成功**。egui の `help: implement the missing item: fn ui(...)` が決定打になった
:::

一発書きの精度だけ見れば egui/iced は「最低」ですが、**修正ループは1ラウンドで全て収束**しました。第2回・第3回と同じく、「一発書きの精度」ではなく「**復旧ループの回りやすさ**」で評価すべきだということが、GUIでも再現したわけです。

# ビルドと配布 — GUI選定を支配する観点

ここがGUI選定の本丸です。実測した依存数・クリーンビルド時間・バイナリサイズです。

| crate | ユニーク依存数 | release クリーンビルド | release バイナリ | 外部依存 |
|---|---|---|---|---|
| iced | 191 | 20.4s | 10.2 MB | Pure Rust |
| egui (eframe) | 208 | 22.2s | 11.5 MB | Pure Rust |
| tauri | 272 | 28.5s | **8.1 MB** | OS WebView・要Xcode |
| slint | 335 | 32.7s | **8.4 MB** | Pure Rust |

時刻crate（第3回は依存2〜15）と比べ、GUIは **191〜335依存** と桁違いに重く、クリーンビルドは20〜33秒、バイナリは8〜11.5MBです。バイナリは tauri / slint が小さめ。tauri はOSのWebViewを使うためブラウザを同梱せず、Web技術系でも肥大しません。

## C依存の壁: gtk4 はそもそもビルドできない

第2回で diesel が `ld: library 'pq' not found` でリンク失敗したのと同じ構図が、GUIではさらに大規模に起きます。pkg-config も GTK4 も入っていない素のmacOSで gtk4 をビルドすると——

```
warning: glib-sys@0.22.6: Could not run `pkg-config --libs --cflags glib-2.0 ...`
  The pkg-config command could not be found.
error: failed to run custom build command for `glib-sys`
（gobject-sys / gio-sys / gdk-pixbuf-sys / pango-sys / cairo-sys-rs /
  graphene-sys / gdk4-sys / gsk4-sys / gtk4-sys も同様に失敗）
```

**10個の `*-sys` crateが軒並み pkg-config と GTK 一式を要求して全滅**します。AIエージェントにとって、これは「コードを直す」では解決しない、環境構築のループに迷い込む最悪のパターンです。一方、egui / iced / slint は Pure Rust なので、`cargo build` が通る環境さえあれば**環境構築ごとAIに任せられます**。

## tauri は中間 — 設定とアセットの段取り

tauri は依存こそ Pure Rust 寄りですが、最小のカウンタをビルドするだけでも **アイコン画像・`tauri.conf.json`・`build.rs`・フロントエンドのHTML・OS WebView(Xcode)** が必要です。実際、本検証でも「`dist` が無い」「`icon.png` が無い」で2回足踏みしてからビルドが通りました。`cargo tauri` のような専用CLIで雛形を生成するのが前提になる理由がここにあります。「初速」の観点では、Pure Rust の egui/iced/slint に一歩譲ります。

# ちょっとしたTodoで見る「整いやすさ」

カウンタのような最小例では差が出ませんが、実用アプリでは「**整ったものがどれだけ手早く作れるか**」が効きます。同じ仕様のTodo（テキスト入力・追加・完了チェック・削除・残数表示）を egui / iced / slint で書き、デフォルトの見た目を比べました。

| | egui | iced | slint |
|---|---|---|---|
| デフォルトテーマ | 素朴（ダーク） | 整ったライト | モダン（ネイティブ風） |
| 日本語表示 | **豆腐（□）になる** | デフォルトでOK | デフォルトでOK |
| 標準ウィジェット | 入力/チェック/ボタン一通り | 一通り＋装飾済み | 一通り＋装飾済み |

![egui Todo（日本語が豆腐）](/images/gui-todo-egui.png)
*egui: 既定フォントにCJKグリフが無く、日本語が □ になる*

![iced Todo](/images/gui-todo-iced.png)
*iced: 既定で日本語が出て、青いチェック・ボタンで整う*

![slint Todo](/images/gui-todo-slint.png)
*slint: モダンな既定テーマ。日本語もそのまま*

最大の差は **日本語表示** でした。**egui は既定フォントにCJKグリフを含まず、日本語が豆腐（□）になります**。表示するには `FontDefinitions` でフォントを自前で登録する一手間が要ります。

```rust
// egui で日本語を出すための一手間
let mut fonts = egui::FontDefinitions::default();
let bytes = std::fs::read("/System/Library/Fonts/ヒラギノ角ゴシック W3.ttc")?;
fonts.font_data.insert("jp".into(), egui::FontData::from_owned(bytes).into());
fonts.families
    .entry(egui::FontFamily::Proportional)
    .or_default()
    .insert(0, "jp".into());
ctx.set_fonts(fonts);
```

これを入れれば egui でも日本語は出ます。

![egui Todo（フォント設定後）](/images/gui-todo-egui-jp.png)
*egui: フォントを埋め込めば日本語表示（完了項目は打ち消し線）*

一方 **iced / slint は既定でシステムの日本語フォントを使い、整ったテーマも標準**です。「Todoを作って」と頼んでそのまま日本語で見られる状態になるのは iced / slint。egui は「即時モードで書きやすいが、見た目を整えるにはフォントとテーマの設定が要る」という位置づけです。tauri は HTML/CSS なので、整えやすさは普段のWeb制作の資産がそのまま効きます。

「**整いやすさ**（手早く、整った画面になるか）」という観点では、**既定で整って日本語も出る iced / slint が有利**で、egui は一手間ぶん割り引く、と評価できます。これも「AIに書かせて、どれだけ早くまともな画面になるか」という連載の視点の一部です。

# 推奨: 用途で割り切る

GUIは用途で正解が変わります。AIに書かせる前提では、次の整理が実務的です。

- **ツール・可視化・社内用の素早いGUI** → **egui**（即時モードで単純、Pure Rustで環境を選ばない。ただし日本語表示にはフォント設定が要る）
- **きちんと状態管理したいデスクトップアプリ／整った日本語UIを最小手数で** → **iced**（Elmアーキテクチャで規律が出て、既定で整う）
- **組込み・独自デザインシステム／既定の見た目を重視** → **slint**（宣言的DSL。既定テーマが最もモダン。ただしライセンス3択に注意）
- **Webフロント資産がある・配布を重視** → **tauri**（Web技術＋小さいバイナリ。段取りは多い）
- **既存のGTK資産がある時だけ** → **gtk4**（C依存の環境構築コストを許容できる場合）

そして共通して、**どれも一発書きは外す前提で、「コンパイラのエラーを返して直させる」運用**にすること。最新版を使うなら、AIに「このバージョンで」と明示し、エラーが出たら臆せず投げ返すのが速い、というのが実測からの教訓です。

# まとめ: GUIは「環境」と「修正ループ」で選ぶ

- GUIは進化が速く、**最新版ではAIの一発書きが外れやすい**（egui/iced とも 0/5）。「即時モードだから書きやすい」より「学習データがそのバージョンに追いついているか」が効く
- だが**失敗は全てコンパイルエラーで、ヒントを返せば1ラウンドで復旧**できた。一発書きの精度表だけで選ぶと判断を誤る
- 選定を支配するのは **ビルドが通る環境**。Pure Rust の egui/iced/slint は素で通り、gtk4 は GTK 一式が無いと一歩も進めない。tauri はその中間

# 連載総括: crate選定とは「AIの修正ループがどこで止まるか」を選ぶこと

全4回を振り返ると、一本の筋が通っていました。**crate選定とは、AIが「書く→ビルド→直す」を回すとき、どこで止まり、どこで守られるかを選ぶこと**だったのです。

| 回 | テーマ | AIが「止まる / 守られる」ポイント |
|---|---|---|
| 1 | デファクト群 | 学習データが厚く、そもそも止まりにくい。デファクトを外す時だけ理由を問う |
| 2 | Web / DB | **コンパイル時検証**（sqlx/diesel）がスキーマ不整合を弾く＝レビューの肩代わり |
| 3 | 時刻 | TZ/DSTの意味論は型で守れるが、`since`の向きのような取り違えは**テスト**が要る |
| 4 | GUI | **ビルドが通る環境**を作れるか。新APIでも**復旧ループが回る**か |

ここから、連載全体を貫く3つの結論にたどり着きます。

1. **「一発書きの精度」だけで選ばない。** 一発目を外しても、コンパイラやビルドが具体的なエラーを返して**修正ループが1〜2回で収束する**crateが、実務では強い。第2回のDB、第4回のGUIで繰り返し確認できました。
2. **C依存・外部依存はAIエージェント最大の足かせ。** 第2回の libpq、第4回の GTK は同じ構図です。Pure Rust なら環境構築ごとAIに任せられます。
3. **人間レビュアーの役割は「観点で問う」ことに移った。** コードの正しさはコンパイラと修正ループに委ね、人間は8観点——とりわけ **ビルド・定番接続・保守・捨てやすさ**——で、AIの選定に「なぜそれを選んだのか」を問う。これが、Rustを普段書かない人がAIのライブラリ選定をチェックする、いちばん費用対効果の高い関わり方です。

第1回の冒頭で、「AIで自分用のツールを作れるようになったが、ライブラリ選定の良し悪しが分からない」という不安から始めました。その答えは、全部を理解することではありません。**AIの修正ループがどこで止まるかを見抜く8つの観点を持ち、要所で「なぜ」を問う**こと。それだけで、AIに任せたRustのコードは、ぐっと信頼できるものになります。

ここまで読んでいただき、ありがとうございました。
